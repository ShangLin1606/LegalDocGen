version: "3.9"
services:
  api:
    build: .
    ports:
      - "8001:8001"
    environment:
      - MLFLOW_TRACKING_URI=/app/mlruns
      - CHROMA_PERSIST_DIR=/app/data/chroma
    volumes:
      - ./mlruns:/app/mlruns
      - ./data:/app/data
